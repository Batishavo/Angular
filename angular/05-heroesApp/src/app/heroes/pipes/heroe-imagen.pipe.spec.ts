import { HeroeImagenPipe } from './heroe-imagen.pipe';

describe('HeroeImagenPipe', () => {
  it('create an instance', () => {
    const pipe = new HeroeImagenPipe();
    expect(pipe).toBeTruthy();
  });
});
